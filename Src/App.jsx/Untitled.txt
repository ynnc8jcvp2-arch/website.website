import React, { useState, useEffect, useMemo } from 'react';
import { 
  Shield, FileText, Activity, AlertCircle, Download, Clock, 
  CheckCircle2, Menu, X, ArrowRight, Layers, CheckSquare, 
  Info, MessageSquare, Landmark, Calculator, BadgeCheck, 
  Loader2, Copy, Check, Factory, Briefcase, Users
} from 'lucide-react';

const apiKey = ""; 

async function fetchAutomatedContent(prompt, systemInstruction = "", jsonSchema = null) {
  const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`;
  const payload = {
    contents: [{ parts: [{ text: prompt }] }],
    systemInstruction: { parts: [{ text: systemInstruction }] },
  };
  if (jsonSchema) {
    payload.generationConfig = { responseMimeType: "application/json", responseSchema: jsonSchema };
  }
  const delays = [1000, 2000, 4000, 8000, 16000];
  for (let i = 0; i <= delays.length; i++) {
    try {
      const response = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if (!response.ok) throw new Error(`HTTP Error: ${response.status}`);
      const data = await response.json();
      return data.candidates?.[0]?.content?.parts?.[0]?.text;
    } catch (error) {
      if (i === delays.length) throw error;
      await new Promise(resolve => setTimeout(resolve, delays[i]));
    }
  }
}

const AdUnit = ({ className = "h-[90px]", label = "Financing Advertisement Space" }) => (
  <div className={`bg-slate-50 border border-slate-200 flex flex-col items-center justify-center text-slate-400 rounded-xl relative overflow-hidden w-full ${className}`}>
    <span className="text-[9px] font-bold uppercase tracking-widest absolute top-3 left-4 opacity-40">Sponsored Placement</span>
    <span className="text-sm font-medium opacity-50 font-sans tracking-tight">{label}</span>
  </div>
);

function pmt(rate, nper, pv) {
  if (rate === 0) return pv / nper;
  return (rate * pv * Math.pow(1 + rate, nper)) / (Math.pow(1 + rate, nper) - 1);
}

export default function App() {
  const [page, setPage] = useState('home');
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [amount, setAmount] = useState("450000");
  const [rateStr, setRateStr] = useState("10.5");
  const [years, setYears] = useState("10");
  const [isManufacturer, setIsManufacturer] = useState(false);
  const [copiedCell, setCopiedCell] = useState(null);

  const principal = parseFloat(amount.replace(/,/g, "")) || 0;
  const mr = (parseFloat(rateStr) / 100) / 12;
  const n = parseInt(years) * 12;
  const monthly = useMemo(() => principal > 0 && n > 0 ? pmt(mr, n, principal) : 0, [principal, mr, n]);
  const totalRepaid = monthly * n;
  const totalInterest = totalRepaid - principal;
  const finalSBAFee = isManufacturer ? 0 : principal * 0.0275;

  const fmt = (v) => v.toLocaleString("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 });
  const fmt2 = (v) => v.toLocaleString("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 2, maximumFractionDigits: 2 });

  const handleCopy = (text, id) => {
    const textArea = document.createElement("textarea");
    textArea.value = text;
    textArea.style.position = "fixed";
    document.body.appendChild(textArea);
    textArea.focus();
    textArea.select();
    document.execCommand('copy');
    document.body.removeChild(textArea);
    setCopiedCell(id);
    setTimeout(() => setCopiedCell(null), 1500);
  };

  return (
    <div className="min-h-screen bg-slate-50 text-slate-900 font-sans">
      <header className="bg-slate-900 border-b border-slate-800 sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
          <div className="flex items-center gap-4 cursor-pointer" onClick={() => setPage('home')}>
            <div className="w-8 h-8 rounded-lg bg-white flex items-center justify-center text-slate-900"><Landmark className="w-5 h-5" /></div>
            <span className="text-xl font-bold text-white font-serif">ClearPath SBA</span>
          </div>
          <nav className="hidden md:flex gap-4">
            <button onClick={() => setPage('home')} className="text-sm text-white px-4 py-2 rounded-lg bg-slate-800">Overview</button>
            <button onClick={() => setPage('calculator')} className="text-sm text-slate-300 px-4 py-2 hover:text-white">Pricing Terminal</button>
          </nav>
        </div>
      </header>

      <main className="max-w-7xl mx-auto px-6 py-12">
        {page === 'home' ? (
          <div className="space-y-12">
            <div className="bg-slate-900 p-12 rounded-[40px] text-white">
              <h1 className="text-5xl font-serif font-bold mb-6">Structure Deals. Close Faster.</h1>
              <p className="text-slate-400 text-xl mb-8">The institutional utility for the commercial capital ecosystem.</p>
              <button onClick={() => setPage('calculator')} className="px-8 py-4 bg-emerald-600 rounded-xl font-bold">Launch Pricing Engine</button>
            </div>
          </div>
        ) : (
          <div className="grid lg:grid-cols-12 gap-12">
            <div className="lg:col-span-5 space-y-8">
              <div className="bg-white p-8 rounded-3xl border border-slate-200 shadow-sm">
                <label className="block text-xs font-bold uppercase text-slate-500 mb-4">Requested Capital</label>
                <input type="text" value={amount} onChange={e => setAmount(e.target.value)} className="w-full text-3xl font-bold outline-none border-b-2 border-slate-100 focus:border-slate-900 transition-all pb-2" />
                <div className="mt-8">
                   <label className="flex items-center gap-4 cursor-pointer">
                     <input type="checkbox" checked={isManufacturer} onChange={e => setIsManufacturer(e.target.checked)} className="w-5 h-5" />
                     <span className="text-sm font-bold uppercase">Apply FY26 Manufacturer Waiver</span>
                   </label>
                </div>
              </div>
            </div>
            <div className="lg:col-span-7">
               <div className="bg-slate-900 p-12 rounded-[48px] text-white relative overflow-hidden">
                 <div className="relative z-10">
                   <div className="text-xs uppercase font-bold text-slate-400 mb-4">Monthly Payment</div>
                   <div className="text-7xl font-bold mb-8">{fmt2(monthly)}</div>
                   <div className="grid grid-cols-2 gap-8">
                     <div className="bg-slate-800 p-6 rounded-2xl">
                       <div className="text-xs text-slate-400 mb-2 uppercase">Total Interest</div>
                       <div className="text-2xl font-bold">{fmt(totalInterest)}</div>
                     </div>
                     <div className="bg-slate-800 p-6 rounded-2xl">
                       <div className="text-xs text-slate-400 mb-2 uppercase">SBA Fee</div>
                       <div className="text-2xl font-bold">{fmt(finalSBAFee)}</div>
                     </div>
                   </div>
                 </div>
               </div>
            </div>
          </div>
        )}
      </main>
    </div>
  );
}